<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.3.2</version>
	</parent>
	<groupId>com.example</groupId>
	<artifactId>sea</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>sea</name>
	<description>sea</description>
	<packaging>pom</packaging>

	<modules>
		<module>sea-common</module>
		<module>sea-gateway</module>
		<module>sea-media</module>
		<module>sea-poi</module>
		<module>sea-code</module>
		<module>sea-system</module>
		<module>sea-oauth2</module>
		<module>sea-auth</module>
		<module>sea-log</module>
		<module>sea-workflow</module>
		<module>sea-notification</module>
	</modules>
	<properties>
		<java.version>17</java.version>
		<mybatis-plus-start.version>3.5.10.1</mybatis-plus-start.version>
		<druid.version>1.2.24</druid.version>
		<spring-cloud-alibaba.version>2023.0.1.0</spring-cloud-alibaba.version>
		<spring.cloud-version>2023.0.1</spring.cloud-version>
		<mapstruct-version>1.5.5.Final</mapstruct-version>
		<lombok-mapstruct-binding-version>0.2.0</lombok-mapstruct-binding-version>
		<springdoc.version>2.6.0</springdoc.version>
		<flowable.version>6.8.0</flowable.version>
		<aliyun-core.version>4.6.3</aliyun-core.version>
		<aliyun-dysmsapi.version>1.1.0</aliyun-dysmsapi.version>
		<!-- 注：本期 SMS 供应商 SDK 还未在生产接入，pom 暂不打包 dysmsapi，运行时按需启用 -->
	</properties>
	<dependencies>
		<!-- https://mvnrepository.com/artifact/org.projectlombok/lombok -->
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<scope>provided</scope>
		</dependency>

		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-annotations</artifactId>
		</dependency>

	</dependencies>


	<dependencyManagement>
		<dependencies>
			<!-- https://mvnrepository.com/artifact/com.alibaba/druid -->
			<dependency>
				<groupId>com.alibaba</groupId>
				<artifactId>druid</artifactId>
				<version>${druid.version}</version>
			</dependency>

			<!-- 字段映射工具 -->
			<dependency>
				<groupId>org.mapstruct</groupId>
				<artifactId>mapstruct</artifactId>
				<version>${mapstruct-version}</version>
			</dependency>
			<dependency>
				<groupId>org.mapstruct</groupId>
				<artifactId>mapstruct-processor</artifactId>
				<version>${mapstruct-version}</version>
				<scope>provided</scope>
			</dependency>
			<dependency>
				<groupId>org.projectlombok</groupId>
				<artifactId>lombok-mapstruct-binding</artifactId>
				<version>${lombok-mapstruct-binding-version}</version>
			</dependency>


			<dependency>
				<groupId>org.springframework.cloud</groupId>
				<artifactId>spring-cloud-dependencies</artifactId>
				<version>${spring.cloud-version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<dependency>
				<groupId>com.alibaba.cloud</groupId>
				<artifactId>spring-cloud-alibaba-dependencies</artifactId>
				<version>${spring-cloud-alibaba.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			
			<!-- SpringDoc OpenAPI -->
			<dependency>
				<groupId>org.springdoc</groupId>
				<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
				<version>${springdoc.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springdoc</groupId>
				<artifactId>springdoc-openapi-starter-webflux-ui</artifactId>
				<version>${springdoc.version}</version>
			</dependency>

			<!-- flowable / 阿里云 SDK 版本号留在 properties（${flowable.version} / ${aliyun-*.version}）；
			     dependencyManagement 下沉到各自子 pom，子模块 dependencies 引用时不写 version -->
		</dependencies>
	</dependencyManagement>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<skipTests>true</skipTests>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>

</project>
