<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.sea.system.dao.SysMenuMapper">

    <select id="selectMenuListByUserId" resultType="com.example.sea.system.entity.SysMenu">
        SELECT sm.* FROM sys_menu sm
        INNER JOIN sys_role_menu srm ON sm.id = srm.menu_id
        INNER JOIN sys_user_role sur ON srm.role_id = sur.role_id
        WHERE sm.del_flag = 0 AND sm.visible = '1' AND sm.`status` = '1'
            AND sur.user_id = #{userId}
        ORDER BY sm.order_num,sm.create_time ASC
    </select>

</mapper>
